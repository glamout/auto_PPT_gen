项目指南
1.项目过程，不要随意更改和提示词相关的内容，如果涉及要修改，请优先和用户确认。
2.在发生调用时，包括前端与后端的交互调用，或者前端与第三方的调用，请记录调用方的调用信息，包括调用的接口、参数，记录返回方的返回信息，包括返回的接口、参数。
3.zenmux 的调用 prompt 请保持和原来使用 google provider（即原google aistudio）的一致
4.原来除了首页外，其他页面上的元素除了有必要增加 logs 的按钮外，都保持不变。如果需要改变，需要和用户确认。